{% extends 'dashboard.html.twig' %}

{% block title %}Edit Product{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow"> <!-- Ajout d'une ombre pour plus de profondeur -->
                <div class="card-header bg-primary text-white"> <!-- Utilisation de couleurs pour distinguer la tête de carte -->
                    Edit Product
                </div>
                <div class="card-body">
                    {{ include('product/_form.html.twig', {'button_label': 'Update'}) }}
                </div>
                <div class="card-footer d-flex justify-content-between">
                    <a href="{{ path('app_product_index') }}" class="btn btn-outline-primary">Back to list</a>
                    {{ include('product/_delete_form.html.twig', {
                        'button_class': 'btn btn-danger',
                        'button_label': 'Delete'
                    }) }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
